hosts:
  - name: sage75
    data_iface: ens33
    m0_servers:
      - runs_confd: true
      - io_disks: { path_glob: "/dev/sd[b-i]" }
    m0_clients:
        s3: 0
        other: 2
  - name: sage75c1
    data_iface: ens33
    m0_servers:
      - runs_confd: true
      - io_disks: { path_glob: "/dev/sd[b-i]" }
    m0_clients:
        s3: 0
        other: 2
pools:
  - name: the pool
    disks: all
    data_units: 1
    parity_units: 0
    # allowed_failures: { site: 0, rack: 0, encl: 0, ctrl: 0, disk: 0 }
