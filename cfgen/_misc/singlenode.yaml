hosts:
  - name: localhost
    disks: { path_glob: "/dev/loop[0-9]*" }
    m0_servers:
      - endpoint: { proto: lo, portal: 34, tmid: 101 }
        runs_confd: true
        io_disks: { path_regex: . }
    c0_clients: []
    m0t1fs_clients: []
pools:
  - name: the pool
    # allowed_failures: { site: 0, rack: 0, encl: 0, ctrl: 0, disk: 0 }
    data_units: 1
    parity_units: 0
    disks: all
