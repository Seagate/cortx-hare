hare:
  post_install:
    cmd: $HOME/seagate/cortx/hare/bin/hare_setup post_install
    args: --config $URL --report-unavailable-features --configure-logrotate
  init:
    cmd: $HOME/seagate/cortx/hare/bin/hare_setup init
    args: --config $URL
  config:
    cmd: $HOME/seagate/cortx/hare/bin/hare_setup config
    args: --config $URL
  test:
    cmd: $HOME/seagate/cortx/hare/bin/hare_setup test
    args: --config $URL --plan $TYPE
  reset:
    cmd: $HOME/seagate/cortx/hare/bin/hare_setup reset
    args: --config $URL
  cleanup:
    cmd: $HOME/seagate/cortx/hare/bin/hare_setup cleanup
    args: --config $URL --pre-factory
  prepare:
    cmd: $HOME/seagate/cortx/hare/bin/hare_setup prepare
    args: --config $URL
  pre-upgrade:
    cmd: $HOME/seagate/cortx/hare/bin/hare_setup pre-upgrade
    args: --config $URL
  post-upgrade:
    cmd: $HOME/seagate/cortx/hare/bin/hare_setup post-upgrade
    args: --config $URL
support_bundle:
  - $HOME/seagate/cortx/hare/bin/hare_setup support_bundle
